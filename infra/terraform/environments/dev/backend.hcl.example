bucket       = "REPLACE_WITH_REMOTE_STATE_BUCKET"
key          = "REPLACE_WITH_REMOTE_STATE_KEY"
region       = "REPLACE_WITH_STATE_BUCKET_REGION"
encrypt      = true
use_lockfile = true

# Backend bucket, state key, region and role ARN are injected by the protected
# GitHub environment or local operator. No AWS identifier is committed here.
