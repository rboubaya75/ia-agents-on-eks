aws_region = "REPLACE_WITH_TARGET_AWS_REGION"

name_prefix = "iaagents"

embedding_profile_alias    = "REPLACE_WITH_SERVER_PROFILE_ALIAS"
embedding_profile_revision = "REPLACE_WITH_IMMUTABLE_PROFILE_REVISION"
embedding_dimensions       = 1024
vector_distance_metric     = "cosine"
vector_index_generation    = "g001"
vector_encryption_revision = "enc-v1"

# During an index migration, add the previous immutable contract here before
# changing vector_index_generation. Remove it only in a separate retirement PR.
retained_vector_index_contracts = {}

encryption_mode = "AES256"
create_kms_key  = false
kms_key_arn     = null

tags = {
  CostCenter = "REPLACE_WITH_COST_CENTER"
  Owner      = "REPLACE_WITH_OWNER"
}
