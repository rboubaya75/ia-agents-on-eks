[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

[project]
name = "ia-observability"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["pydantic==2.13.4"]

[tool.hatch.build.targets.wheel]
packages = ["src/ia_observability"]
